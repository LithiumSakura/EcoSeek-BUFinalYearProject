runtime: python311

instance_class: F1

env_variables:
  GOOGLE_CLOUD_PROJECT: 'ecoseek-buproject-f015d'
  SECRET_KEY: 'pineapple-cherry-kuromi-scrunchie-cards'

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto

automatic_scaling:
  min_instances: 0
  max_instances: 2
